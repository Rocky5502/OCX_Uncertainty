# Public Data Layer

`manifests/` contains stable task identities, selection metadata, and hashes.
It contains no third-party repository source, reference implementation, hidden
test, credential, personal path, or participant record.

Reconstruct source-bearing inputs from official benchmark distributions using
the scripts documented in `docs/DATASETS.md`.
